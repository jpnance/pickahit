extends layout.pug

block title
	+title('All Sessions')

block content
	table
		thead
			tr
				th Username
				th Logins
				th Logouts
			each user in users
				tr
					td #{user.username}
					td #{sessionUserActivityMap[user.username].active}
					td #{sessionUserActivityMap[user.username].inactive}
